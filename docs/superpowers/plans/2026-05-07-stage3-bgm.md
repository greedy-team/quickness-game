# Stage 3 BGM Implementation Plan (revised: phase-gated)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stage 3 standalone 모드에서 `phase === 'running'` 동안에만 `bgm_stage_3.mp3`를 재생한다. 인트로/결과 직후/Stage 4 split 모드에서는 무음.

**Architecture:** 글로벌 라우트 기반 `BgmController`는 `/stage/3`에서 무음을 유지하고(`TRACK_TO_FILE.stage3 = null`), `Stage3Game` 컴포넌트가 자체 `<audio>` 엘리먼트를 phase/mode 기반 effect로 직접 제어한다. 두 메커니즘이 같은 라우트에서 절대 동시에 재생하지 않도록 가드.

**Tech Stack:** React 19, Vite 8. 테스트 러너 없음 → 검증은 `npm run build` + 브라우저 수동.

---

## File Structure

| 파일 | 역할 | 변경 |
|------|------|------|
| `public/assets/sounds/bgm_stage_3.mp3` | 신규 BGM 음원 | 이미 존재 |
| `src/assets.js` | 자산 경로 레지스트리 | `bgmStage3` 키 추가 (Task 1) |
| `src/audio/trackRegistry.js` | 트랙 ID → 파일 매핑 | `stage3 = null` 유지 (변경 없음) |
| `src/audio/BgmController.jsx` | 라우트 기반 BGM | 변경 없음 |
| `src/stages/stage3/Stage3Game.jsx` | Stage 3 진입점 | `<audio>` + phase 기반 effect 추가 (Task 2) |

총 코드 변경 2파일.

---

## Task 1: `bgmStage3` 자산 키 등록

**Files:**
- Modify: `src/assets.js`

- [x] **Step 1: `bgmStage3` 키 추가**

`src/assets.js`의 `sounds` 객체에 추가:
```js
sounds: {
  bgm:       '/assets/sounds/bgm.mp3',
  bgmStage3: '/assets/sounds/bgm_stage_3.mp3',
  openDoor:  '/assets/sounds/open_door_sound.mp3',
  endingAliveSfx:      null,
  endingSilhouetteSfx: null,
}
```

- [x] **Step 2: 모듈 로드 검증**
```bash
node -e "import('./src/assets.js').then(m => console.log(m.ASSETS.sounds.bgmStage3))"
```
Expected: `/assets/sounds/bgm_stage_3.mp3`

- [x] **Step 3: 커밋**
```bash
git add src/assets.js
git commit -m "feat: register bgmStage3 sound asset key"
```

---

## Task 2: `Stage3Game`에 phase 기반 audio 추가

**Files:**
- Modify: `src/stages/stage3/Stage3Game.jsx`

- [x] **Step 1: import 추가**

상단 import 블록에 추가:
```jsx
import { useCallback, useEffect, useRef, useState } from 'react';
import { ASSETS } from '../../assets.js';
import { BGM_DEFAULTS } from '../../audio/trackRegistry.js';
```

- [x] **Step 2: audioRef 선언**

`useState('idle')` 다음 줄에 추가:
```jsx
const audioRef = useRef(null);
```

- [x] **Step 3: phase 기반 재생 effect 추가**

split prop watch effect 다음에 추가:
```jsx
// BGM: standalone 모드의 running phase에서만 재생.
// 라우트 기반 BgmController 대신 phase 기반 로컬 제어 — split 모드에서는
// 다른 분할과의 오디오 충돌 방지 위해 비활성.
useEffect(() => {
  const audio = audioRef.current;
  if (!audio) return;

  if (mode === 'standalone' && phase === 'running') {
    audio.volume = BGM_DEFAULTS.volume;
    audio.loop = BGM_DEFAULTS.loop;
    audio.play().catch(() => {});
  } else {
    audio.pause();
    audio.currentTime = 0;
  }
}, [mode, phase]);
```

- [x] **Step 4: JSX에 audio 엘리먼트 추가**

return JSX 마지막 자식으로 추가 (standalone 모드에서만 마운트):
```jsx
{mode === 'standalone' && (
  <audio ref={audioRef} src={ASSETS.sounds.bgmStage3} preload="auto" />
)}
```

- [x] **Step 5: 빌드 검증**
```bash
npm run build
```
Expected: `vite build` 성공.

- [x] **Step 6: 커밋**
```bash
git add src/audio/trackRegistry.js src/stages/stage3/Stage3Game.jsx
git commit -m "feat: gate stage3 BGM to running phase only"
```

> Note: trackRegistry.js는 라우트 기반 BGM을 잠시 활성화했다가 본 결정으로 다시 `null`로 되돌리는 변경이 함께 들어감.

---

## Task 3: 브라우저 수동 검증

**Files:** 없음

- [ ] **Step 1: dev 서버 기동**
```bash
npm run dev
```
브라우저에서 `http://localhost:5173` 열기.

- [ ] **Step 2: 9개 시나리오 통과 확인**

| # | 시나리오 | 기대 결과 |
|---|---------|----------|
| 1 | Hub → Stage 3 진입 (인트로 화면) | hub BGM 정지, 인트로 무음 |
| 2 | 인트로에서 Space 누름 | stage3 BGM 재생 시작 |
| 3 | 게임 플레이 도중 | BGM 루프 지속 |
| 4 | 게임 종료 | BGM 즉시 정지 |
| 5 | 자동 Hub 복귀 | hub BGM 복귀 |
| 6 | 새로고침 후 Stage 3 직접 진입 | 인트로 무음 |
| 7 | 새로고침 후 Space 입력 | BGM 재생 (gesture로 autoplay 차단 해제) |
| 8 | Stage 4 진행 중 Stage 3 분할 표시 | 무음 (split 가드) |
| 9 | Stage 1·2 진입 | 무음 (회귀 없음) |

- [ ] **Step 3: dev 서버 종료**
```bash
# Ctrl+C
```

---

## Self-Review

1. **Spec coverage 체크**
   - §3.1 phase 기반 재생 → Task 2 Step 3 effect
   - §3.2 standalone 한정 → Task 2 Step 4 (audio 마운트 가드) + Step 3 (effect 내 mode 체크)
   - §3.3 글로벌 컨벤션 유지 → trackRegistry.js `null` 유지
   - §3.4 자산 키 → Task 1
   - §3.5 BGM_DEFAULTS → Task 2 Step 3
   - §4.1 assets.js → Task 1
   - §4.2 trackRegistry.js → 변경 없음
   - §4.3 Stage3Game.jsx → Task 2
   - §6 9개 시나리오 → Task 3
   - §8 완료 기준 → 모두 매핑됨

2. **Placeholder 스캔**: 없음. 모든 코드 블록 즉시 적용 가능.

3. **Type/이름 일관성**:
   - `bgmStage3` (Task 1) ↔ `ASSETS.sounds.bgmStage3` (Task 2) 일치
   - `BGM_DEFAULTS` import 경로 ↔ trackRegistry.js export 일치
   - `mode === 'standalone'` 가드: effect, JSX 양쪽 일관 적용

이슈 없음.
